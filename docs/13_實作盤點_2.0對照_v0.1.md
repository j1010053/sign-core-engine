# 實作盤點 × 架構 2.0 對照(v0.1)— docs/13

> **性質**:2026-07-13 讀入架構 2.0(彙整 P1–P19、鳥瞰、修補05 P20–P28)後,
> 對 M0 步驟 1–7 已實作內容的總檢查。三個問題:什麼已經是 2.0 的、什麼要改名歸位、什麼是缺口。
> 路線圖權威 = 《架構2.0總鳥瞰》§4;本檔不重複定義,只做對照。
> **2026-07-30 同步註記**:本檔的逐項表仍是 2026-07-13 歷史盤點；Step 13–16
> 後續狀態以 docs/20、docs/22、docs/26 為準。

---

## 1. 總判定

**M0 產出(dsl/core/cli 三 crate)= 2.0 圖中的「Grammar Engine + 獨立音變 DSL(P20 路徑 A)」,
原樣存活,無需重寫。** 2.0 的新東西(Language/Compile/ChangeSet 四層)全部是**包在外面的新 crate**,
不動 M0 內核——這正是修補01「引擎核心一行不改」宣稱的兌現,且 P20 把它升級為產品定位
(DSL 可獨立發布)。

## 2. 已實作 × 2.0 元件對照

| 2.0 元件(鳥瞰圖) | 現有實作 | 狀態 |
|---|---|---|
| **Grammar Engine**(單規則生命週期) | `core::lifecycle`(五步、B5 凍結共享 match)+ `primitives`(六原語)+ `verbs`(組合式動詞 + I12/I13 音段通道)+ `scan` + `spellout`(C11 純函數 + phrase hook)+ `locality` + `diag`(B9) | 🟢 完成,8.1–8.6 全綠;**P20 路徑 B 將原樣復用** |
| **獨立音變 DSL(路徑 A)** | `dsl` crate(規則檔 parser/lowering/executor)+ `cli`(`(規則檔,詞表)→詞表′` 契約)| 🟢 完成;Lexurgy 黃金子集 8/8;**P20 依賴方向天然滿足**(dsl 不知 sign) |
| **臨時韻律域 Word(P1)** | `repr::Word` 七層 + `domain`(I14);**`language::word`(步驟 12,I18):組合樹 → build_phrase → stage 切片驅動 → 表層**;cophonology M1 音段子集 | 🟢 完成(巢狀環深度序/旋律 cophonology = 步驟 18) |
| **stage 錨定(P3)** | `stage:` 標記 parse+記錄(I14 關鍵詞) | 🟡 **無 dispatch 行為**——P18(stage dispatch 優先於書寫順序)未實作,現 executor 純書寫順序;落點 = 步驟 10 的 Stage 排序 pass |
| **Compiled Grammar(P8)** | `language::codegen`(步驟 11,I17):`CompiledGrammar { phon_source, program }`——phon 側 = dsl 可直接吃的規則集原文(P20 §1.3),Program 由 `tshiatun_dsl::compile` 產出;stage dispatch 已由 ④ 排序編碼於書寫序 | 🟢 完成;雙軌迴歸 8.1–8.6 全綠(P20 §1.4) |
| **決定性(P26 前半)** | interner(SymId/ValId)純序列配發 ✓;無隨機/時間源(I4)✓ | 🟢 既有規範已對齊;sign 級 ID 配發屬步驟 13+ |
| **Definition/Rule 二分(P9)** | 宣告(Feature/Symbol/Class/Melody/Parse)與規則(`=>`)在 parser 已是兩類 | 🟡 語意合;**表面語法未遷**:現行 Lexurgy 形 `Feature voice(+voice,-voice)`,2.0 為 `feature voice = (+,-)`(修補05 §10.1)。見 §4 裁決點 1 |
| **else 鏈(P22)** | 未實作(docs/02 §13 開放項因 P22 關閉,機制移到 else) | 🔴 缺口;屬步驟 9(language 條件文法)+ dsl 域回移 |
| **tier 相對 adjacency(P22)** | scan 的序列相鄰已做(8.6 Meeussen);一般規則環境仍線性 | 🟡 「下放給一般環境」待做 |
| **Path / 守衛(P22)** | 無 | 🔴 步驟 9+ |
| **Language / Compile / ChangeSet / 四層 / State** | 無(設計層) | 🔴 = M1/M2 全部;新 crate `language`、`changeset` |

## 3. 既有 I 系列決策的 2.0 相容性

| 決策 | 判定 |
|---|---|
| I1–I8(表徵/commit/拓撲) | 🟢 不受影響(Grammar Engine 內部) |
| I9/I12/I13(音段通道/刪除連鎖) | 🟢 不受影響 |
| I11 v2(origin 原位記憶) | 🟢 不受影響;且是 D6 的正確實作 |
| I14(stage 關鍵詞 + domain 模組) | 🟢 與 P3/P18 用詞一致;自定域 = 七層宣告的載體 |
| I5(哨兵/corpus) | 🟢 繼續有效;完整匯入器仍屬後續 |

## 4. 裁決結果(擁有者 2026-07-13)

1. **語法遷移邊界(已裁決)**:`feature`/`symbol`/`class`(音段/特徵系統宣告)**留 dsl 域,
   維持 Lexurgy 形**;Language 的 Definition(`=`)**只管 dsl 不認識的概念**——prosody/
   受控範疇/trait/global/sign。**同名不同域靠檔案區域(語境)區分,非關鍵字區分**
   (規則檔區域 = dsl 文法;.lang 的 Definition 區 = language 文法)。
2. **crate 佈局(已裁決)**:`build_word` 與循環套用的**協調邏輯**在 language crate
   (依賴 dsl 型別);executor/lifecycle/primitives/verbs **留 dsl 側不動**。
   接口:`language::build_word / compile_phon_rules → dsl::Word / RuleSet → dsl::run`,
   單向依賴。(dsl 保留現有簡版 build_word 供路徑 A 獨立模式。)
3. **docs 回填(已裁決)**:不逐檔回填;**以最新檔案(彙整+鳥瞰+修補05)為基準**,
   舊文件出入處以其為準。

## 5. 進度

- **步驟 8 完成**(2026-07-13):`crates/language` 落地(五組節點/P27 Block/P28 root/
  P21 printer/P26 id/I15);dump golden 釘住格式;P20 依賴守衛測試已在 dsl。
- **步驟 9 完成**(2026-07-13):Language Parser(dsl 域=檔案區域判定、==Block、
  Name[n]、=/=>、@stage、else 鏈、Path 文法);round-trip 恆等 + 正規化不動點 + AST golden。
- **引擎分離完成**(2026-07-17):core/dsl/cli + examples + corpus → 獨立 repo
  **`../tshiatun`**(Tshiatūn,GPL-3.0-or-later,`.qy`,I7 v2);本 repo 剩設計文件 +
  `crates/language`(dep → `../../../tshiatun/crates/dsl`)。
- **步驟 10 完成**(2026-07-17,I16):`language::compile` ①–④(trait 展開/後者勝
  解析/stage 排序;每 pass dump golden + round-trip;四類 CompileError;P5/P6/P18 落地)。
  P18 dispatch 在 ④ 以排序實現(§2 表列 stage 缺口就此關閉,executor 端不需改)。
- **步驟 11 完成**(2026-07-20,I17):`language::codegen` ⑤——Compiled Grammar
  (phon_source + Program,P8 無 trait/priority 痕跡)+ Compiled Sign(③ 後者勝欄位
  + 局部規則)。🔑 **雙軌迴歸 8.1–8.6 全綠**(P20 §1.4:表層逐字相同 + 逐步 Word
  全狀態相同 + 步數相同)= 共時側 2.0 化完成。CLI 功能回測 11 案新編
  (`crates/language/tests/cli_functional.rs`,住工作台,擁有者裁決)。
- **引擎同步**(2026-07-20):submodule `51f1a30`→`520e0c8`;CLI 測試契約更新;
  examples 內容零變(僅空白),fixtures 免重移植;零規則缺陷上游已修(§6)。
- **步驟 12 完成**(2026-07-20,I18):`language::word` 組合樹 → 臨時 Word →
  P3 stage 切片驅動 → 表層;cophonology M1 音段子集(顯式拒絕出界);
  🔑 詞根+詞綴組合+surface sandhi 首測全綠(8 案,oracle=上游 stages.rs)。
  **M1 共時側閉環達成**。
- **M1++ 封板(2026-07-21,修補07 P38–P44)**:12a 單一維度中立分類樹 + 四維
  projection/validation → 12b construction/slots + typed Rust `SlotMap` → 12c 完整
  四維 derived token + 遞迴 filler/provenance → 12d Sign/token 規則三分 + phon
  Then/Else → 12e validated Patch/diff + meta data。
- **步驟 12a**:`OntologyRegistry` 單樹；菱形去重、循環安全、遠祖→近祖、同層後寫
  `belongs`、本地最後。`ValidationReport` 提供分級代碼、Def winner-first provenance
  warning 與 slot conflict error；有效 Def/slot/rule 皆進 runtime Sign。
- **步驟 12b**:`construction::apply_with` 接 stored Sign 或 derived token；`SlotMap`
  支援 preserve/rename/autofill/internalize/optional override，先驗證再原子套用；保留
  德語變位既有回歸。封板後已補 `.lang` 平坦 `map SLOT OP [ARG]`，source 與 Rust
  API 共用 `SlotMapOp`，並由 compile 驗證未知 slot/filler、重複操作與範疇不符。
- **語法重設計 + 單一分類樹**(2026-07-21,I22;修訂 P38→v0.2):Language 表面語法
  改 **colon+縮排**(取代 `{ }`);容器頭 `sign/trait/global trait Name:`(無
  `syn trait`);**統一 body**(trait ≡ sign,維度區塊 `syn:/phon:/sem:/prag:` +
  `slots:`);**單一維度中立分類樹**(`OntologyRegistry` 單樹,syn/sem/phon/prag =
  內容面向)。全回歸綠(雙軌/德語/compile/codegen/roundtrip golden 重生)。
- **步驟 12c**:`DerivedToken` 攜 phon/syn/sem/prag、分類、殘餘 slots、遞迴
  `TokenProvenance`；`SemNode` role 保持節點組合。
- **步驟 12d**:`run_sign_dim_rules`/`run_token_dim_rules` 共用 Matched/Unmatched/Error；
  未知 category/path Error 不落 Else；Then Error 保留先前 commit 後停止。規則與分支
  保存實體行號；phon Then/Else 直接排放 Tshiatūn，並有 `.lang`/`.qy` differential。
- **步驟 12e = M1++ 封板**:`Patch` 私有欄位 + validated builder/parser + per-dim
  `diff`；entrenchment/lexicalized 僅資料。`compile_system` 為真實公共入口；
  `tests/m1pp_counterexamples.rs`、`tests/m1pp_system.rs` 與
  `scripts/verify-m1pp.ps1` 提供反例、端到端與本機封板證據。
- **共時 `.lang` surface 補齊(2026-07-21)**:維度內 Def lhs 接完整 Path
  (`.`/`[key]`/`~tier`)；`origin/provenance/lifecycle` 成為 typed、受驗證的 sign
  頂層資料；SlotMap source→runtime 貫通。年代/文本/可信度仍明確屬 Evidence，未混入
  Language。貼合矩陣見 docs/14。
- **stdlib Grambank 子集(2026-07-21)**:依修補06建立 `core|grambank/{code,data,config}`
  套件；Grambank v1.0 先釘選 25 個主要二元參數。每項提供未知根 trait、明確
  `_Absent`(0)、`_Present`(1)，並將可安全推得的構式行為投影至 syn/sem/prag；
  不夾帶規則、slot、語言觀測值或動力學。公開入口自動載入，詳見 docs/15。
- **direct stored-filler `slot_features`（共時能力已驗證）**：construction 可從 frozen
  predicate slot 讀 `subject_case`／`object_case`，為單次 stored-sign occurrence 注入
  已宣告的 syn enum feature，再以其 read-only realization guard 選 phon。English
  `see` + 同一 `she` sign 的 subject/object 導出 `she sees her`（`/she/`／`/her/`），
  且 lexical source 不變；固定值衝突會拒絕。這不擴充為 derived-token downward
  forwarding，也不會在注入後重跑 filler syn/sem/prag rules。
- **2026-07-22 Step 13 語義／API 封板回歸**：根 workspace **251/251**（`conlang-language` 220、`conlang-changeset` 31）、Tshiatūn **157/157**；全部 0 failed／ignored／filtered，根 workspace 各 harness 皆有非零發現。完整工具閘門仍為基礎設施 exit 2，不宣稱 release gate exit 0。
  通過；它是已列共時能力的證據，不表示 M1+ 已完整封板。
- **步驟 13(M2 歷時來源層)已完成**：Primitive Edit 四原語（P23
  Insert/Delete/Update/Move）、sidecar stable ID（P26）、typed `NodeRef`／Path resolver、
  stable Anchor、獨立 validator 與 Language-wide diff/trace 均已落地；證據集中於
  `docs/20_Step13_PrimitiveEdit與SourceIdentity_v1.md`。
- **後續已完成**：Step 14 的 statement transaction／allocator rollback／replay／
  lazy compile、Step 15 Atomic Rewrite，以及 Step 16 EvolutionGraph／diff／reconstruct
  均已封板；P60/P64 共享物件庫與 node folder 亦由獨立 host persistence crate 落地。
  下一個歷時層是 Step 17 Recipe／Goal runtime；現行契約見 docs/26。

## 6. 已知缺陷

(無。CLI 零規則丟失輸入詞已由上游 tshiatun `520e0c8` 修復,submodule bump 後
`cli_zero_rules_is_identity` 改斷言詞表′恆等,2026-07-20。)
