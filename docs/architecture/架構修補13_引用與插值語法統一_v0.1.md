# 架構修補 13 — 引用與插值語法統一 v0.1

> **狀態:已實作並編號(擁有者裁定)。** 本檔 §3 的 **P72–P80** 為權威定義;
> 其餘 P 系列的權威掛載點見 CLAUDE.md §0。
>
> 承 P24(引用是 Ref 屬性值)、P41(valence = slots)、P42(construction = Sign)、
> P44(每維規則只改自己那維)、P71(Def 路徑封閉清單)。
> 影響面:`.lang` 表面語法、canonical printer、`base_source` 與套件 digest。

## 1. 問題陳述

`$self` / `$slot.` 的解析原本**沒有單一實作**。八個位置各自以 `strip_prefix`
比對,接受的子集互不相同:

| 位置 | 接受的形 | 維度 | 路徑長度 |
|---|---|---|---|
| 維度規則 RHS / guard | `$self.<dim>.<path>` / `$slot.<n>.<dim>.<path>` | 四維 | 任意 |
| `slot_features:` 值 | 只 `$slot.<n>.syn.<f>` | 鎖死 syn | 恰 1 段 |
| `constraints:` 欄位運算元 | `$slot.` **可省** | 鎖死 syn | 恰 1 段 |
| `constraints:` 順序運算元 | **完全不解析**,整串當 slot 名 | — | — |
| case scrutinee | `$self.` **可省**,省略後首段被當 slot | `.phon` 重載(見後果 4) | 恰 2 段 |
| `token_scalar` | `$self.` **可省** | 四維 | 恰 2 段 |
| application 實參 | `{$self}` / `{$slot.n}` / `{n}` | — | — |
| phon 模板 / sem role | **只有** `{n}` 裸名 | — | — |

三個後果:

1. **同一件事多種拼法。** `stem.syn.number` 與 `$slot.stem.syn.number` 在
   constraint 位置等價;`{stem}` 與 `{$slot.stem}` 在實參位置等價,但在 phon
   模板位置只有前者合法。
2. **首段猜測。** 裸形之下,`stem.phon`(讀 slot)與 `syn.number`(讀自己)
   只能靠「首段是不是維度關鍵字」決定主體——一個**名為 `syn` 的 slot 會被
   靜默讀成自己的 syn 維**。
3. **正確性靠記憶力維持。** rename 需要三個重寫器(裸形 / `$slot.` / `{…}`),
   而「哪個 `SignItem` 該套哪幾個」是一張人工維護的對應表。實測其中一格是錯的:
   constraint 運算元寫成顯式 `$slot.` 形時 **rename 完全不會改到它**,留下懸空引用。
4. **`.phon` 被重載成兩種運算。** 同一個 `<主體>.<維度>` 記法,`$slot.N.phon`
   **有**欄位時比欄位值、**沒有**欄位時改比本體樹成員關係。兩種語意靠「有沒有
   欄位」這個難以察覺的差異區分,而範疇比對在全庫早有自己的記法(`[Trait]`)。
   同時 `$slot.N.<非 phon 維>.<欄位>` 靜默永不匹配——不是規格如此,是實作債:
   規格中唯一相關的出現是一行**範例**,而 `FillerSnapshot` 一直帶著四維欄位。

`{…}` 的處理另外散在七處,其中三處是幾乎相同的字元迴圈,只差命中後做什麼。

## 2. 裁定的語法

```text
Reference     := Subject ( '.' Dim ( '.' Path )? )?
Subject       := '$self' | '$slot' '.' Name
Interpolation := '{' Subject '}'
Dim           := 'phon' | 'syn' | 'sem' | 'prag'
Path          := Name ( '.' Name )*     (承 P22 / 修補05 §3.5,經 P80 縮減)
```

**兩個記號是正交算子,不是競爭的拼法:**

- **`$` 只建立引用,不求值。** `$slot.stem.syn.number` 是一個指名——「slot `stem`
  的填充者的 syn.number 這個欄位」。規則拿它去比對或寫入。
- **`{…}` 要求求值後把結果嵌在這裡。** `/{$slot.stem}+s/` 是「把 `stem` 的填充者
  算出來,結果放這個位置」。

兩者可以疊:**括號內永遠是一個引用**。`{$self}` 因此不是特例,而是這條規則的
一般情形;`{stem}` 才是漏掉 `$` 的那個。

括號內只寫主體、不帶維度與路徑——**嵌入什麼型別由括號所在的位置決定**
(phon 模板要 surface、sem role 要語意節點),不由引用自己宣告。

## 3. 決策點

### P72 主體一律顯式,裸寫法不相容

**選擇**:`$` 不可省略,`{…}` 內也一樣。舊的四種裸寫法一律拒絕:

| 舊 | 新 |
|---|---|
| `before(subject, object)` | `before($slot.subject, $slot.object)` |
| `equal(a.syn.number, b.syn.number)` | `equal($slot.a.syn.number, $slot.b.syn.number)` |
| `case stem.phon:` | `case $slot.stem.phon:` |
| `case syn.number:` | `case $self.syn.number:` |
| `/{stem}/`、`agent = {subject}`、`f({stem})` | `/{$slot.stem}/`、`agent = {$slot.subject}`、`f({$slot.stem})` |

**理由**:裸形是「首段猜測」的唯一成因(見 P73),也是三個 rewriter 的唯一成因
(見 P74)。保留相容等於保留那兩個問題。

**代價**:`base_source` 與套件 digest 全數改變,既有 `.chg` 需 rebase。
**可逆性**:低。回退需再一次全庫遷移與 re-bless。

### P73 移除首段猜測

**選擇**:刪除「首段是維度關鍵字 → `$self`,否則 → 同名 slot」這條消歧規則。

**理由**:它使 slot 命名空間與維度關鍵字相撞。一個名為 `syn`/`phon`/`sem`/`prag`
的 slot 在裸形下不可定址,且失敗方式是**靜默讀到別的東西**而非報錯。

**證據**:`reference_surface.rs::a_slot_named_after_a_dimension_is_addressable`
——`$slot.syn.phon` 現在確實指向名為 `syn` 的 slot。

### P74 rename 收斂為單一掃描

**選擇**:刪除 `rewrite_slot_operand` / `rewrite_slot_accesses` /
`rewrite_slot_template` 三個重寫器與其呼叫點對應表,改為單一
`reference::rename_slot`。

**理由**:主體顯式之後引用只有 `$slot.<name>` 一種可掃形狀,而 `{$slot.<name>}`
也是同一形狀——整串運算元、內嵌於本文、模板插值三者可共用同一次掃描。

**附帶修正**:constraint 運算元的顯式 `$slot.` 形先前不被 rename,已修;
迴歸測試 `primitive_edits.rs::slot_rename_rewrites_an_explicitly_sigilled_constraint_operand`
已驗證在舊行為下會紅。

### P75 括號內容不帶維度與路徑

**選擇**:`{…}` 內只允許 `$self` 或 `$slot.<name>`,不允許 `{$slot.x.syn.f}`。

**理由**:`{}` 的語意是「求值並嵌入」,而嵌入什麼型別是**位置決定**的。
若允許括號內帶維度,同一個位置就會有兩個型別來源(位置與引用),必須再定
優先序。

**未決**:`{$self}` 寫在 phon 模板裡(會遞迴)目前落到 unknown-slot 診斷,
而非專屬錯誤碼。見 §5。

#### 增修 A:構式內部不回指構式本身(擁有者裁定)

`{…}` 的內容是主體,而主體有兩個。哪一個在哪個位置合法,由**這條原則**決定:

> `$self` 可以當「讀欄位的主體」或「往外傳的引數」,**不能當「填進自己內部的
> 值」**。

三個 `{…}` 位置因此分成兩邊:

| 位置 | `{$self}` | 理由 |
|---|---|---|
| phon 模板 `/{$self}/` | **非法** | 模板**就是**這個 sign 的形式;嵌自己的 surface 進自己的 surface,無條件遞迴 |
| sem role `agent = {$self}` | **非法** | 角色的填充者是某個 slot,不會是構式自己 |
| application 實參 `f({$self})` | **合法** | 不是內部回指,是把自己往**外**傳給另一個構式(`en_3sg({$self})` 在用) |

**兩個不誤傷的邊界**(已釘測試):

- **反身構式**不受影響。「他洗他自己」是兩個 role 綁**同一個 slot**
  (`agent = {$slot.subj}`、`patient = {$slot.subj}`),不是綁構式自己。
- **自我套用**(`word({$self})`,`f` 就是自己)本來就由 `APPLICATION_CYCLE`
  擋下,不需要新規則。

**落地的是訊息,不是行為。** 三種情形先前就已各自被拒絕/接受,但兩處訊息
說錯了原因:phon 的 `{$self}` 報「找不到叫 `$self` 的 slot」(會讓作者去找一個
不存在的 slot),sem role 的只講形狀不講回指。兩處已改為指出真正的原因,且
「真的未知的 slot」仍走 `CONSTRUCTION_TEMPLATE_UNKNOWN_SLOT`——兩種失敗現在
分得開。

出口:`reference_surface.rs` 的
`a_self_interpolation_in_a_phon_template_names_the_recursion` /
`a_role_cannot_be_filled_by_the_construction_itself` /
`two_roles_sharing_one_slot_is_still_legal` /
`passing_self_as_an_argument_to_another_construction_is_legal` /
`passing_self_to_itself_is_a_cycle`。

### P76 `{…}` 掃描收斂為單一實作

**選擇**:`scan_interpolations` / `substitute_interpolations` /
`render_interpolation` 三個函數取代原本散在七處的括號處理(其中三處是同型
字元迴圈)。

**保留的分工**:括號**良構性**(未閉合、巢狀、空)仍由 `template_references`
檢查,括號**內容**的良構性交給 reference。兩者是不同的錯誤類別。

### P77 呼叫點宣告子集,不自行比對

**選擇**:每個位置宣告一份 `RefSpec`(允許哪種主體、維度政策、路徑政策),
由單一 `reference::parse` 執行。目前五份公開 spec:
`SLOT_SYN_FEATURE` / `CONSTRAINT_OPERAND` / `CONSTRAINT_SLOT` / `SCRUTINEE` /
`SUBJECT_ONLY`。

**理由**:接受/拒絕的集合成為可讀的宣告,而非散落的 `strip_prefix` 串接。
新增一個位置時,「它接受什麼」是一次宣告而不是一次重新實作。

### P78 兩處行為收緊,一處技術債移除,一處記法去重

**收緊(兩處)**:

- **非法 scrutinee 是 compile 錯誤**(新增 `CASE_INVALID_SCRUTINEE`)。先前
  `case head.phon:` 能通過 compile,只在求值時靜默不匹配。
- **constraint 運算元解析失敗不再退回「把整串當 slot 名」**。該 fallback 會讓
  裸寫法靜默續存,使 P72 的遷移落空。

**移除技術債(一處)**:case scrutinee 先前只處理 `$slot.NAME.phon`,
`$slot.NAME.<其他維>.<欄位>` 靜默永不匹配。查證後:**沒有任何規格規定
scrutinee 只收 `.phon`**——規格中唯一相關的出現是
`共時lang_FP_expression_typed_case_constraint_v2.md` 的一行**範例**,不是規則。
而 `FillerSnapshot` 一直帶著四維的純量欄位,只是 scrutinee 沒去讀。
現已補上:`$slot.NAME.<dim>.<field>` 讀填充者的純量欄位,與
`$self.<dim>.<field>` 對稱。

**記法去重(一處;擁有者裁定,推翻本檔 v0.1 初稿)**:
`$slot.NAME.phon`(無欄位)先前**兼差**當範疇比對——同一個 `<主體>.<維度>`
記法,有欄位時比欄位值、沒欄位時比本體樹成員關係。已移除。

理由:範疇比對在全庫早有記法,就是 `[Trait]`——slot 約束 `head [RefEntity]`、
function 參數 `verb [Verb]`、guard `$self == [Trait]`、`$slot.NAME == [Trait]`
(`synchronic::Guard::SlotIsA`,早已實作)皆然。讓 `.phon` 再兼一個,等於同一
件事有兩種寫法,且第二種靠「有沒有欄位」這個難以察覺的差異來區分。

**現在的分工**:

| 要比什麼 | 寫法 |
|---|---|
| 欄位的**值** | `case $slot.NAME.<dim>.<field>:` + `== VALUE:` |
| 本體樹的**範疇** | `case:` + `$slot.NAME == [Trait]:` |

`SCRUTINEE` spec 的路徑政策因此改為**必填**——`$slot.head.phon` 缺欄位,在
**compile 期**就被擋下,而不是通過編譯後在求值時才炸。連帶移除已無適用情形的
`CASE_UNKNOWN_CATEGORY` 診斷(equals 分支不再拿 trait 名當期望值)。

出口:`reference_surface.rs` 的
`a_dimension_without_a_field_is_not_a_category_test` /
`a_category_test_uses_the_bracket_guard_form` /
`a_slot_scrutinee_may_read_a_scalar_field_not_only_phon`,
以及 `reference.rs::a_scrutinee_without_a_field_is_rejected_by_the_spec`。

### P79 `.chg` function 層併入同一套主體記法

**選擇**:function guard 的主體改為顯式 `$<參數名>`,並把求值從「文字代換成
`$self`」改為**在具名綁定環境上求值**。連言 `&&` 一併收進 guard 文法。

```text
Subject := '$self' | '$slot' '.' Name | '$' Name
```

`self` 與 `slot` 是保留字,故 `$x` 不會與前兩形相撞。

**先前的作法與它的必然後果**:主體靠 `guard_subjects()` **掃描哪個識別字剛好
在 bindings 裡**,找到後**文字代換成 `$self`**,再交給 `.lang` 的
`guard_matches_sign(sign, guard, registry)`——那個簽名只有**一個** sign。
於是:

- 跨參數的 guard 表達不出來,只能以 `FUNCTION_GUARD_MULTI_SUBJECT` 拒絕;
- 直接寫 `$self` 會得到「沒有主體」(它不是被綁的名字);
- 「哪個識別字是主體」無法在詞法層判定,得先有綁定表。

**單主體不是一條規則,是那條管線的必然結果。** 故解除它與引入 sigil 是同一件事:
求值器吃環境之後 `$x` 才解析得了,而一旦吃環境,多主體就是免費的。

**同時發現並修掉的**:`&&` 先前由**六個消費端各自 `split("&&")`**,guard 文法
本身只認單一比較——function 層完全不認得它(`x… && y…` 會被解析成一個
右端是垃圾字串的比較)。連言因此收進 `parse_guard`,成為 `Guard::All`。

**新增的能力**:

| 形 | 意義 |
|---|---|
| `$x.<dim>.<path> == 值` | 參數的欄位比字面值 |
| `$x == [Trait]` | 參數的範疇 |
| `$x.<dim>.<path> == $y.<dim>.<path>` | **比較兩個參數的欄位** |
| `$x.<dim>.<path> == $y` | 右端是純量參數 |
| `A && B` | 連言,兩端可以是不同主體 |

**型別要求依角色而定**:當主體用的(路徑頭、範疇測試)必須綁到 sign;當純量
值用的(右端裸 `$y`)綁的是字面值。`guard_binding_roles` 把兩者分開,呼叫端
據此決定該報 `GUARD_SUBJECT_NOT_A_SIGN` 還是直接取值。

**代價**:`.chg` 的 guard 語法變更。遷移量實測極小——std 的 `.chg`
**沒有任何**主體路徑 guard(`goals.chg` 只用 `path(...)` 內建),全部 29 處
都在測試字串裡。`FUNCTION_GUARD_MULTI_SUBJECT` 錯誤變體移除。

**仍未做**:function guard 定址 slot(`cxn.slot[agent].…`)。實測那從未可用
——失敗原因是「維度必須是第二段」,與 `[key]` 無關(見 §6.5 的交界後果)。
且 `.lang` 側也沒有「別的 sign 的 slot」這種記法,故它是一個**從未在任何地方
存在過**的功能,該當獨立需求看。

### P80 `Path` 縮減為點分名段

**選擇**:language 域的 Path 文法縮減為

```text
Path := Name ( '.' Name )*
```

原有的 `'[' Key ']'`(slot／序數)與 `'~' TierRef`(聯結)兩種段移除。

**理由**(量測依據見 §6,四條皆為實測而非推論):

- 全庫 201 個 `$` 引用與所有 Def lhs,**零處**用到這兩種段;
- 查找端(`project(dim).get(&path)`、`FillerSnapshot::scalar`)是**字串鍵比對**,
  不解讀結構——三種段的表達力完全相同,`[k]` 只是鍵名裡的字元;
- P71 之後自造欄位一律走 `feature:`(單一識別字),多段路徑的唯一來源是套件
  座標前綴,那些也不用這兩種段——**零生產者**;
- Step 13 的欄位定址本來就只收具名段,其餘段是死分支(已移除)。

**範圍**:只縮減 language 域。序數與 tier 在它們真正有語意的地方不受影響
——`.qy` 的 `LinearTemplate`、Scan 的序數 `[n]`(D16)、旋律層的 tier 記法,
都是 dsl 域的獨立文法(P20 的域分離)。

**代價**:一個先前可表達的形消失了——修補05 §3.4 的 function guard 範例
`cxn.slot[agent].syn.animate`。詳見 §6.5 的交界後果與 P79。

**可逆性**:中。文法放寬容易,但若要補上**結構語意**(讓 `[k]` 真的做索引)
則是加功能,需要先有使用案例。

## 4. 落地紀錄

| | |
|---|---|
| 新增 | `crates/language/src/reference.rs`(652 行,含 13 個單元測試) |
| 新增 | `crates/language/tests/reference_surface.rs`(7 個 surface 契約測試) |
| 接上 | 7 個 src 檔、48 處呼叫 |
| 資料遷移 | `.lang` 8 檔 + Rust fixture 15 檔 + md 4 檔;159 行 |
| digest | `tutorials/en-standard-reconstruction/restore.chg` 以 repo 內
  `bless_en_standard_restore` 重生(該工具正確警告 statements 亦變動) |
| 回歸 | `cargo test --workspace --exclude langcraft-desktop` **995 綠、exit 0** |
| clippy | 警告數與基線相同(language 7 / changeset 140,皆為既有的
  `Err`-variant-too-large) |
| 引擎 | tshiatun 零觸動 |

分兩次提交:第一次為**行為不變**的重構(單一 parser、canonical 逐字不變、
digest 不動),第二次為本檔 P72–P78 的語法變更。

## 5. 未決事項(Phase 2)

以下三項皆為**新功能**而非回歸,語料庫零使用,留待有使用案例時再處理。

1. **function guard 定址 slot**:`.lang` 與 `.chg` 都沒有「另一個 sign 的
   slot」這種記法(`$slot.N` 恆為自身相對)。求值路徑需要 construction
   runtime 的 `apply` 機制才能知道 filler 是誰,guard 求值時沒有那個
   context。見 §6.5。
2. **`.lang` guard 的右端**:P79 讓 `.chg` 的 `==` 右端可以是引用
   (`$x.f == $y.f`);`.lang` 的 `$self.f`／`$slot.x.f` 右端仍只收字面值。
   語料庫該形零使用(跨主體比較目前走值位置的 `require(a, b)`),故未一併
   放寬——但這是一處**已知的不對稱**,有需求時應一起處理。
3. **function guard 定址參數的 slot filler 欄位**
   (`$x.slot.agent.syn.case`):多主體 guard 已可用(P79),但 slot filler
   查詢需要先查 sign 的 slot 定義、再在 filler 上查欄位——這整條求值路徑
   從未存在過。與第 1 項相關但不相同:第 1 項是自身的 slot,本項是參數的
   slot。

## 6. 附錄:`Path` 段的行為量測

量測程式 `crates/language/tests/reference_path_probe.rs`(3 個測試,結果已以
assertion 釘住;`-- --nocapture` 可看逐項輸出)。

### 6.1 語料庫實際用到的形狀

全庫(`.lang` / `.chg` / Rust fixture / md)共 **201 個 `$` 引用**:

| 面向 | 分佈 |
|---|---|
| 維度 | syn 184、phon 10、sem 7、**prag 0** |
| 路徑段數 | 1 段 **191**、0 段 10(`$slot.N.phon` scrutinee) |
| 段型別 | 純識別字 **191**、`[鍵]` **0**、`~tier` **0** |

**沒有任何一處用到多段、`[鍵]` 或 `~tier`。**

### 6.2 查找端怎麼用這條路徑

`read_self` 是 `sign.project(dim).get(&"<dim>.<path>")`,`read_slot` 是
`FillerSnapshot::scalar(dim, path)`——**兩者都以整條路徑當字串鍵查表**。

量測(`measure_what_the_path_segment_actually_supports`):

| 情形 | 結果 |
|---|---|
| 宣告 `a`,引用 `$self.syn.a` | 讀到 |
| 宣告 `a.b`,引用 `$self.syn.a.b` | 讀到 |
| 宣告 `a[k]`,引用 `$self.syn.a[k]` | 讀到 |
| 宣告 `t~tone`,引用 `$self.syn.t~tone` | 讀到 |
| 宣告 `a`,引用 `$self.syn.a[0]` | **讀不到**(Unmatched) |
| 宣告 `a.b`,引用 `$self.syn.a` | **讀不到** |
| 宣告 `a`,引用 `$self.syn.a.b` | **讀不到** |

**結論:`Path` 段在查找端是不透明字串鍵。** 逐字相同就命中,否則不命中;
`.` / `[k]` / `~tier` 只是鍵名裡的字元,沒有索引、沒有上行、沒有下行。
`[k]` 與 `~tier` 因此**不是**「支援索引與 tier 導覽」,只是允許這些字元出現在
鍵名裡。

### 6.3 什麼形狀的路徑能合法存在

`$slot.` 側走完整 compile(`measure_the_slot_side_and_the_closed_list`),
故受 P71 封閉清單約束:

| 路徑 | 結果 |
|---|---|
| `syn.alignment.a` / `sem.aspect.a` / `prag.evidence.a` | 讀到(三個非 phon 維對稱) |
| `syn.alignment.a.b`(三段) | 讀到 |
| `syn.alignment.a[k]` / `syn.alignment.t~tone` | 讀到 |
| `syn.made-up.a`(不在清單上) | **compile 擋下** |

而封閉清單之外的自造欄位一律走 `feature:`,其名稱是**單一識別字**。
`phon:` 區塊根本不收 `field = value`(承 P71 增修 C)。

**綜合**:多段路徑現實中的唯一來源是**套件座標前綴**
(`syn.alignment.…`、`sem.aspect.…`、`prag.evidence.…` 等 30 條);
`[鍵]` 與 `~tier` 雖可通過驗證,卻**沒有任何生產者**——`feature:` 產不出、
封閉清單的座標也不用它們。

### 6.4 `parse_path` 驗證買到什麼

`PathPolicy::RequiredValidated` 對引用的路徑跑 `parse_path`。既然查找端只做
字串比對,這道驗證的作用**不是**保證可解析,而是把「畸形路徑」從**靜默永不
匹配**變成**明確錯誤**——例如 `$self.syn.a..b` 現在報 `BadPath`,而沒有驗證時
它只會是一個永遠查不到的鍵。這與 P78 的兩處收緊同一性質。

### 6.5 由量測導出的決策點

**P80 的量測依據。** 決策本身見 §3;此處是導出它的數據與被否決的兩案。

裁定前的現況是文法允許三種段(`.名` / `[鍵]` / `~tier`)、任意段數,而:

- 語料庫 191/191 只用單段純識別字,Def lhs 側同樣零使用;
- `[鍵]` 與 `~tier` 零生產者——`feature:` 產不出(名稱是單一識別字),
  封閉清單的套件座標也不用;
- 查找端不解讀結構,故三種段的表達力**完全相同**(都只是鍵名字元);
- Step 13 的欄位定址(`identity.rs`)本來就只收 `Name` 段,其餘段是死分支。

曾考慮但未採納的兩案:**(a) 維持現狀**——成本是文法承諾了查找端沒兌現的
能力,讀 `'[' Key ']'` 會合理預期有索引語意;**(c) 保留文法並補上結構語意**
——那是加功能不是修語法,需要使用案例,目前零。

**落地**:

```text
Path := Name ( '.' Name )*
```

`PathSeg` 只剩 `Name` 一個 variant;`identity.rs` 的死分支移除;
`path.rs` 的模組註解記下移除理由與量測依據。`[序數]` 與 `~tier` 在它們真正
有語意的地方(`.qy` 的 Scan 序數、旋律層 tier)**不受影響**——那是引擎域的
獨立文法,與 `.lang` 的 Path 無關。

出口:`path.rs::key_and_tier_segments_are_no_longer_path_syntax`、
`reference.rs::the_path_is_dotted_names_only`,以及 §6.2 / §6.3 兩個探針改為
釘住「這兩種段現在連解析都不過」。

**與 P22 / 修補05 §3.4–3.5 的關係**:已回填(含 P22 決策列、彙整表、
`CLAUDE.md` 的兩處歷史敘述,以及四份 comparison／implementation 文件)。
P71 Phase 2(套件自行宣告座標)若讓座標形狀改變,應重新檢視本項。

**一個交界後果**:修補05 §3.4 原本的 function guard 範例
`cxn.slot[agent].syn.animate`(「construction 的 agent slot 的填充者的 animate
欄位」)**現已不可表達**。`.lang` 側對應寫法是 `$slot.agent.syn.animate`,但
function guard 的主體是參數而非 slot,故 function 層沒有定址 slot 的記法。
這與 P79 是同一個缺口的兩面:function 層缺的不只是 sigil,還有 slot 定址。
範例已改為可表達的形,缺口記在 §5。
