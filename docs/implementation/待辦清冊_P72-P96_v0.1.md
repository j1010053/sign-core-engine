# 待辦清冊 —— P72–P96 這批工作(v0.1)

> **範圍**:2026-08 這一批共時層工作(分層差異向量 v0.2、P72–P74 年代切片、
> P71 增修 D/E、P75、P76、I30、Q1–Q3、P84–P92 引用語法統一、P93–P96 realization)
> **產生的**與**未清的**項目。不是全專案的總待辦。
>
> **這份是索引,不是權威。** 每項的理由、取捨與細節都住在「權威出處」那一欄
> 指的檔案裡;這裡只回答「還欠什麼、卡在誰身上、下一步是什麼」。
> 重複寫一遍等於製造第二個資訊源(實作原則 3)。
>
> Phase 2(M4 之後)的債另有專屬資料夾與收錄條件,見 [`../phase2/README.md`](../phase2/README.md)。

---

## 1. 待裁定 —— 需要擁有者決定才能動

| # | 項目 | 現況 | 建議 | 權威出處 |
|---|---|---|---|---|
| **★1** | **補充形機制(填充者選擇)** | `is`/`does` 是模板裡的字串常量,不是 sign | 選項 1(填充者選擇);邏輯是 `derive_candidates` 的鏡像 | [realization §5](realization收縮與規則分支_v0.1.md) |
| 2 | **Q4 義項並列同名** | `ontology.rs:958` 後寫勝,無警告 | Error + 提示改名(`name` 是 `derive_sense` 的定址鍵,撞名無法消歧) | 並列分歧合併表 |
| 3 | **Q6 `RoleDecl` 並列分歧** | `ontology.rs:923` 後寫勝 | 跟 Q2:收窄放行、放寬 Error(role 與 slot 同型,框架繼承幾乎必然特化角色約束) | 同上 |
| 4 | **Q6 `RoleBinding` 並列分歧** | `ontology.rs:934` 後寫勝 | Error(一個 role 只對應一個 slot;`agent` 不能同時是 subject 和 object) | 同上 |
| 5 | **`when:` 在 phon 的累積語意** | 本輪保守排除 | 帶規則之後未必錯(那正是 PFM 的 rule block),但須**刻意**裁定 | [realization §5](realization收縮與規則分支_v0.1.md) |
| 6 | **B1 覆蓋缺口** | 語料 3 處缺陷只抓到 1 處 | 讓檢查也走帶槽的 trait;需先想清楚與「trait 是分類節點」的分工交界、以及重複報告怎麼避免 | 同上 |
| 7 | **P93–P96 編號確認** | 文件標「擬編」 | 確認後回填《架構修補彙整》與 `CLAUDE.md` §0 | 同上,檔頭橫幅 |

**★1 是這批唯一有連鎖阻塞的一項**:它擋住 §2 的 D6,也擋住「realization 只剩規則」
的終局。可派生形態(規則、加綴)不需要填充者選擇,**補充形需要**——這是它與其他
待裁定項的差別,不是規模差別而是性質差別。

---

## 2. 方向已定,只是還沒做

### 2.1 語料遷移(D)

| # | 對象 | 內容 | 阻塞 |
|---|---|---|---|
| D1 | **新增** `EnglishFiniteVerbForm` | 吃 `stem [Verb]`,規則分支處理**可派生**屈折 | — |
| D2 | `EnglishSVIntransitive` / `EnglishSVOTransitive` | 去掉 `{$slot.predicate}s`,predicate 改填 D1 的產物 | D1 |
| D3 | `EnglishCountNounForm` | 規則分支處理 `mouse`→`mice` | — |
| D4 | `EnglishPluralNP`、std:cxg `PrefixNegation` / `SuffixNegation` | 三處相鄰槽補 `+`(P96) | — |
| D6 | en-standard 四條硬編 `is`/`does` | 歸位到 sign | **★1** |

D4 的三處**都是真缺陷**,但診斷只抓得到第一處(見待裁定 6)——修的時候別只看警告。

### 2.2 文件(E)

| # | 檔案 | 內容 |
|---|---|---|
| E2 | `tutorials/共時lang語法教學_v1.md:163` | realization 段改寫(分支 = base + rules;循環/後循環分層) |
| E3 | `docs/implementation/chg_authoring_insert_update_v0.md:81,224` | realization 定址表(branch → rule 那一層) |

### 2.3 測試(F)

| # | 內容 | 為何值得補 |
|---|---|---|
| F3 | 巢狀構式的循環順序 | 內層實現形餵外層(`construction.rs:1753`)目前**只有產品路徑在用,沒有測試直接釘住**。這層一旦壞掉,表現是外層拿到未實現的深層形——安靜且難查 |

### 2.4 實作缺口

| # | 內容 | 說明 |
|---|---|---|
| G1 | stored filler / occurrence 路徑不能跑分支規則 | 兩條路徑拿不到 phon program,帶規則的分支在那裡**明確報錯**(不靜默丟規則)。那是 P93 未覆蓋的形狀,與 ★1 同一批處理較合理 |

---

## 3. 上游依賴(Tshiatūn)

| # | 項目 | 狀態 |
|---|---|---|
| U1 | **reduplication 需要 capture** | 1→N 擴張的輸出端必須是字面符號序列;「複製匹配到的內容」(Lexurgy 的 `$1`)在 qy 只保留於轉換層,**未降階實作**。重疊構詞要等這個 |
| U2 | `Romanizer-<name>` 中間羅馬化 | 上游 defer,需要多輸出欄的概念。realization 產出的是音韻串,書寫形式是更下游的事,短期不擋 |

本批依賴的上游能力(插入語法、方向敏感邊界、1→N 擴張)已在 `3105363` 驗證,
紀錄見 [realization §4](realization收縮與規則分支_v0.1.md)。

---

## 4. 已結清(避免重複追)

| 項目 | 結果 |
|---|---|
| P 編號撞號協調 | 引用分支 P72–P80 → **P84–P92**;語意錨層 P77–P83 已隨 merge 進樹。P75–P92 連續無空洞 |
| 表示塌陷(`ShapeMismatch`) | 已修:phon 模板統一存成 fragment。**不需 rebless** |
| C1 定址 / C3 內容雜湊 | **免費得到**——P93 選了既有的 `DimFragment` 而非新 variant |
| `restore.chg` rebless | 不需要;canonical 文字逐位元未變,identity manifest 不進 library lock digest |
