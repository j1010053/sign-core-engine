[package]
name = "conlang-query"
version = "0.1.0"
edition = "2021"

[dependencies]
conlang-language = { path = "../language" }
conlang-changeset = { path = "../changeset" }
conlang-stats = { path = "../stats" }
serde = { version = "1", features = ["derive"] }
