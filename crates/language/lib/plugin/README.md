# Embedded plugin mount

Packages placed here use the same `code/data/config` contract as std and
natural-language libraries. Runtime filesystem discovery and host bridges are
intentionally deferred; the current catalog embeds explicitly registered
packages only.
