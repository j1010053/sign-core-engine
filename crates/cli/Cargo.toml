[package]
name = "conlang-cli"
version = "0.1.0"
edition = "2021"
description = "M0 acceptance CLI: rules file + word list -> derivations with trace"

[[bin]]
name = "conlang"
path = "src/main.rs"

[dependencies]
conlang-core = { path = "../core" }
conlang-dsl = { path = "../dsl" }
