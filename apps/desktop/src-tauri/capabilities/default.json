{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Minimal permissions for the LangCraft main window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "store:default"
  ]
}
